use webdb;

SELECT * FROM tbl_user;

-- 회원가입 진행(INSERT)
insert into TBL_USER(USER_NAME, USER_AGE)
values ('훈이', 6);

-- 전체 회원 조회
select * from TBL_USER;

-- 이름이 훈이인 회원만 조회
select *
from TBL_USER
where USER_NAME = '훈이';

-- 이름과 나이로 조회
select *
from TBL_USER
where USER_NAME = '짱구' and USER_AGE = 5;